# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Replace with your MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGO_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/bookez?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong random secret key (use: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_key_here_replace_with_strong_random_string
JWT_EXPIRE=7d
JWT_COOKIE_EXPIRE=7

# CORS Configuration (Frontend URLs)
# In production, replace with your actual frontend domain
FRONTEND_URL=http://localhost:3000

# Security Note:
# NEVER commit the actual .env file with real credentials to version control
# Always use strong, randomly generated secrets for JWT_SECRET
# Keep your MongoDB credentials secure and rotate them regularly
