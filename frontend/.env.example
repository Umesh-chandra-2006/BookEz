# API Configuration
# Backend API URL - Update this based on your deployment environment
VITE_API_URL=http://localhost:5000/api

# Development
# VITE_API_URL=http://localhost:5000/api

# Production (example)
# VITE_API_URL=https://your-backend-domain.com/api

# Security Note:
# NEVER commit the actual .env file if it contains sensitive data
# In Vite, only variables prefixed with VITE_ are exposed to the client
